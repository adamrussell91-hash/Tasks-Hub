/** Teaching Hub html_app editor reads a default model id. */
export const DEFAULT_ANTHROPIC_MODEL = 'claude-sonnet-4-6';

/** Compact workload judgment — flags only, not task writes. */
export const INTUITIVE_SCAN_MODEL = 'claude-haiku-4-5';
