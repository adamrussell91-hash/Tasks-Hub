# Netlify Functions (runtime — never commit real values)
TASKS_HUB_PASSPHRASE_HASH=
SESSION_SECRET=
SITE_ORIGIN=https://tasks-hub.adam-russell.com

# Optional: bake the Functions origin into the Pages build (no trailing slash).
# VITE_API_BASE_URL=https://tasks-api.adam-russell.com

# Optional — Clare chat + Network judgment + all agents
# ANTHROPIC_API_KEY=
# INTUITIVE_SCAN_MODEL=claude-haiku-4-5

# Optional — agent repo read/write (GitHub Contents). Without this, read_repo_file /
# repo_file mutations explain that the token is missing.
# AGENT_REPO_TOKEN=
# AGENT_REPO=adamrussell91-hash/Tasks-Hub
# AGENT_REPO_BRANCH=main

# Optional — Clare's Life Hub context (energy/mood/upcoming events/active goals).
# Fine-grained GitHub PAT scoped ONLY to adamrussell91-hash/life-hub-data,
# Contents: read-only. Without this Clare works exactly as before, just
# without whole-life context.
# LIFE_HUB_DATA_TOKEN=
# LIFE_HUB_DATA_REPOSITORY=adamrussell91-hash/life-hub-data
# LIFE_HUB_DATA_PATH=central-node.md
# LIFE_HUB_DATA_BRANCH=main

# Optional — machine callers from sibling hubs
# TASKS_HUB_SHARED_SECRET=
